module bind_v_sanduba;

bind sanduba
  v_sanduba sanduba (
  .clock(clock), .reset(reset),
  .r_green(r_green), .r_atum(r_atum), .r_bacon(r_bacon),
  .ea(ea), .pe(pe),
  .m100(m100), .dev(dev),
  .count(count), 
  .d100(d100), .green(green), .atum(atum), .bacon(bacon), .busy(busy)
);

endmodule
